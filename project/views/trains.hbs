<h1>Trains</h1>

{{! READ table }}
<table>
    <thead>
        {{! For the table header row, we print the DB attribute names }}
        <tr>
            {{#each trains.[0]}}
            <th>
                {{@key}}
            </th>
            {{/each}}
        </tr>
    </thead>

    <tbody>
        {{! For each row, print the id, fname, lname, house, blood status, and train that each student it on}}
        {{#each trains}}
        <tr>
            <td>{{this.idTrain}}</td>
            <td>{{this.trainName}}</td>
            <td>{{this.maxCapacity}}</td>
            <td>{{this.onRoute}}</td>
        </tr>
        {{/each}}
    </tbody>
</table>